# ============================================================
# Semantic MCP Server — Environment Configuration
# Copy this file and fill in real values before running.
# NEVER commit the populated version to version control.
# ============================================================

# --- MySQL Connection ---
DB_HOST=localhost
DB_PORT=3306
DB_USER=semantic_user
DB_PASSWORD=CHANGE_ME
DB_META_SCHEMA=semantic_meta
DB_POOL_SIZE=5

# --- MCP Server ---
MCP_PORT=8000

# --- Logging ---
LOG_LEVEL=INFO
